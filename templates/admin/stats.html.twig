{% extends 'base.html.twig' %}

{% block title %}Statistiques{% endblock %}

{% block body %}

    <div {{ react_component('JobFilter', { 'categories': categories, 'technologies': technologies }) }}>
        Loading... <i class="fas fa-cog fa-spin fa-3x"></i>
    </div>

    <div class="container">
        <div class="row">
            <div class="col-12">
                <h1>Etats des compétences</h1>
                {{ render_chart(chartResume, {

                }) }}

            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <h1>Nombre d'offre par métiers</h1>
                {{ render_chart(chartOffers, {

                }) }}
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <h1>Salaire par poste</h1>
                {{ render_chart(chartSalary, {

                }) }}
            </div>
        </div>
        <div class="row">
            <div class="col-12">
                <h1>répartition par age des candidate</h1>
                {{ render_chart(chartUsers, {

                }) }}
            </div>
        </div>
    </div>
{% endblock %}
